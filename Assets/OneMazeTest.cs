using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class OneMazeTest : MonoBehaviour

{
    public GameObject maze;
    // Start is called before the first frame update
    void Start()
    {
        maze.SetActive(true);
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
